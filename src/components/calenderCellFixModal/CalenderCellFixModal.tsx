import React from "react";
import { Modal } from "antd";

interface IProps {
  visible: boolean;
  initialValues: any[];
  handleCancel: () => void;
  handleSubmit: () => void;
}

export const CalenderCellFixModal: React.FC = () => {
  return <div>CalenderCellFixModal</div>;
};
